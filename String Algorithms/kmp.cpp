#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

const int maxN = 1e5;

int pi[maxN], k;
string s, t;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin>>s>>t;

    initPi();

    for(itn i = 0; i < )
}
